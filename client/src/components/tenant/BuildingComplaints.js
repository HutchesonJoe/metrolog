 
function BuildingComplaints(){
  // const tenantBuilding = buildings.find((building)=>building.id===user.apartment.building_id)
    // buildingComplaintInfo = tenantBuilding.tenant_complaints.map((complaint)=><ComplaintCard tenantComplaint={complaint}/>)
  return(
    <div>
      My Building Complaints
    </div>
  )
}

export default BuildingComplaints;